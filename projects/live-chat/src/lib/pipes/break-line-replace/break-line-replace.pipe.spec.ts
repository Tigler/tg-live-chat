import { BreakLineReplacePipe } from './break-line-replace.pipe';

describe('BreakLineReplacePipe', () => {
  it('create an instance', () => {
    const pipe = new BreakLineReplacePipe();
    expect(pipe).toBeTruthy();
  });
});
